function Nav(props) {
    const {sort} = props;

    return (
        <div className="nav">
            tests
            <button onClick={sort}>Sort!</button>
        </div>
    );
}

export default Nav;
